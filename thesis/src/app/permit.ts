export class Permit {
    constructor(
        public prof_id: number,
        public title: string,
        public description: string,
        public from: string,
        public until: string
    ) { }
}
